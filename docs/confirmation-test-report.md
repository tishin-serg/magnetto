# Проверка подбора фильма с подтверждением — 6 сентября 2026

Изменения установлены в работающем боте. Проверка выполнена через Telegram Web
в сохранённой пользовательской сессии.

## Автоматические проверки

Maven verify: 110 тестов, 0 ошибок, 0 падений, 0 пропусков.
Проверены включённые границы размера, некорректные значения, неизвестный размер,
минимум сидов, исключение экранок и явно обозначенных сборников, ранжирование,
обязательное подтверждение, защита от повторного подтверждения и чужого чата,
сохранение настроек и изоляция разовых условий, недоступный S3, inline-выбор
без дополнительной кнопки, объединение повторных inline-событий, проверка
фактического размера и передача MetadataReceived в qBittorrent.

## Проверка в Telegram

- /settings открыл настройки. Значение «от 16» при максимуме 15 отклонено;
  дробное значение 4,5 сохранено. Сохранение проверено после перезапуска приложения.
- Клик по подсказке «Матрица · 1999» открыл подтверждение автоматически.
- «Другой вариант» переключил раздачу.
- Разовый диапазон 20–20 ГБ показал отсутствие совпадений.
- «Выбрать вручную» открыл список раздач с пагинацией.
- Разовое переключение на S3 отразилось в подтверждении; отмена не создала задачу.
- Для Big Buck Bunny задана отдельная заявка: 0–1 ГБ, минимум 1 сид, VPS.
  Бот выбрал раздачу около 0,42 ГБ. До подтверждения в базе было 91 задание.
  После нажатия «Скачать» стало 92 — создано ровно одно новое задание.
- Тестовая задача eeb9171c-ff95-45ae-a36b-8ca30e82c40a перешла в DOWNLOADING
  и затем поставлена на PAUSED_BY_USER через кнопку Telegram.
  Ограничения 0–1 ГБ сохранены отдельно в download_size_policy.
- Пользовательские настройки после проверки: 20–100 ГБ, 5 сидов, домашний ПК.
  Текущая пользовательская загрузка на домашнем ПК продолжала работать.

Полная доставка файла в S3 не запускалась. Выход фактического размера за диапазон
проверен автоматическими тестами; намеренно создавать такую раздачу в рабочем
окружении не потребовалось.

Снимки экранов находятся в target/telegram-confirmation-verified.png,
target/telegram-no-matches-verified.png и target/telegram-s3-confirmation-verified.png.